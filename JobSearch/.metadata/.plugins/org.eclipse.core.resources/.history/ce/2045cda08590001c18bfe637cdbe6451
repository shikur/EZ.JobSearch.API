package com.Ez.JobSearch.JobSearch.Services;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.Ez.JobSearch.JobSearch.Repositories.ClientCVRepository;
import com.Ez.JobSearch.JobSearch.Repositories.ClientcvDao;
import com.Ez.JobSearch.JobSearch.model.ClientCV;

import java.util.Collection;
import java.util.Objects;
import java.util.Optional;


@Service
public class ClientServices {
	
	//private  ClientCVRepositoryImpl clientcvDao = new ClientCVRepositoryImpl();
	@Autowired
	ClientCVRepository clientCVRepository;

		/*
		 * @Autowired public ClientServices(MongoOperations mongoOperations) {
		 * this.mongoOperations = mongoOperations; }
		 */
    
		/*
		 * public ClientCV insertClientCVData(ClientCV clientCV) { return
		 * clientcvDao.insertClientCVData(clientCV); }
		 */
    
		/*
		 * public ClientCV insertClientCVData(ClientCV clientCV) { return
		 * this.clientcvDao.insertClientCVData(clientCV); }
		 */
    
    
    public ClientCV insertClientCVData(ClientCV clientCV) {
        return this.clientCVRepository.save(clientCV);
    }

	/*
	 * public Collection<ClientCV> getAllClientCVInformation() { return
	 * this.clientcvDao.insert(null).getAllClientCVInformation(); }
	 * 
	 * public Optional<ClientCV> getClientCVInformationUsingId(String id) { return
	 * clientcvDao.getClientCVInformationById(id); }
	 * 
	 * public void updateClientCVUsingId(String id, ClientCV clientCV) {
	 * clientcvDao.updateClientCVUsingId(id, clientCV); }
	 * 
	 * public void deleteClientCVUsingId(String id) {
	 * clientcvDao.deleteClientCVUsingId(id); }
	 */


	/*
	 * public String generateSequence(String seqName) {
	 * 
	 * ClientCV counter =
	 * mongoOperations.findAndModify(query(where("_id").is(seqName)), new
	 * Update().inc("seq",1), options().returnNew(true).upsert(true),
	 * ClientCV.class); return !Objects.isNull(counter) ? counter.getCvId() : "1";
	 * 
	 * }
	 */
 
    

  //spring.data.mongodb.host= localhost
 // spring.data.mongodb.port= 27017
  //spring.data.mongodb.database= clientCVDB123
}
